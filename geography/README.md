# Geography

Geography registry and geometry manifest.

The registry holds codes, names, levels and hierarchy. Geometry lives as
pre-simplified static artefacts rather than in the database (ADR-0003).
